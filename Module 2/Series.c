#include <stdio.h>
#include <math.h>
int main(){
    int sum=0,range,x,i,np=-1,element,n;
    printf("Enter the range you want to have sum of -> "); scanf("%d",&range);
    printf("Enter the value of x here -> "); scanf("%d",&x);
    sum = cos(x);
    for (i=0;i<=range;i++){
        n*=np;
    }
    printf("%d is the answer of the expansion.",sum*n);
}
